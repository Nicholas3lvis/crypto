<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto Price Checker</title>
    <link rel="stylesheet" href="main.css">
</head>
<body>
    <div class="container">
        <h1>Cryptocurrency Price</h1>
        <form action="/check" method="POST">
            <input type="text" name="cryptoName" placeholder="Enter cryptocurrency name" required>
            <button type="submit">Check Price</button>
        </form>

        <% if (price) { %>
            <p class="result"><%= crypto %> is currently priced at <%= price %> USD.</p>
        <% } %>

        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
    </div>
</body>
</html>
